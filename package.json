{
  "name": "storhub-cdn",
  "version": "0.1.0",
  "description": "S3 compatible API on Cloudflare Workers backed by GitHub Releases",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "cf-typegen": "wrangler types",
    "check": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250822.0",
    "typescript": "^5.7.3",
    "vitest": "^3.1.0",
    "wrangler": "^4.40.0"
  }
}